# DAレビュー — Week 3 差別化機能

## レビュー日時
2026-04-07

## レビュー対象
ブランチ: `feat/week3-differentiators`
コミット: refactor まで完了（5コミット）

---

## チェックリスト

### バグ・論理ミス
- [x] `compute_epsilon` の計算式は仕様通り（`max(0.05, 0.3 * exp(-0.01 * n))`）
- [x] `select_agents` のシグネチャ変更（`DEFAULT_EPSILON` → `None` デフォルト）が既存テストに影響しないことを確認（既存テストは `epsilon=0.0` または `epsilon=1.0` を明示的に渡しているため問題なし）
- [x] `CausalChainEntry` の ForeignKey（`tasks.task_id`）が正しく定義されている
- [x] `GET /tasks/{task_id}/causal-chain` が存在しないタスクに対して404を返す

### テスト十分性
- [x] εの単調減少テストあり
- [x] εの最小値フロアテストあり
- [x] 計算式の数値テストあり（`test_epsilon_formula`）
- [x] 因果連鎖エントリのDB保存テストあり
- [x] APIエンドポイントの正常系テストあり
- [x] APIエンドポイントの404テストあり
- [x] `_RISK_WEIGHT` の各値テストあり
- [x] `_build_system_prompt` の出力内容テストあり

### セキュリティ
- [x] ユーザー入力のバリデーションは既存のスキーマバリデーターが担保
- [x] SQL インジェクションリスクなし（SQLAlchemy ORM 経由）

### コードの可読性・保守性
- [x] `_record_causal_entry` ヘルパーに切り出し済み
- [x] `_build_system_prompt` ヘルパーに切り出し済み
- [x] `app/config.py` に設定を集約済み

### CLAUDE.md プロセス原則
- [x] mainブランチへの直接コミットなし
- [x] テストファーストで実装（REDフェーズのコミットが存在する）
- [x] 既存43テストが全て通過
- [x] 新規12テストが全て通過（合計55テスト）

---

## 指摘事項

### 軽微（修正不要と判断）

1. **`conftest.py` への `causal_chain` モデルの明示的なインポートがない**
   - `app.main` がインポートされることで自動登録されるため問題なし
   - ただし将来 `conftest.py` が `app.main` から独立した場合に壊れるリスクがある
   - MVP段階では許容範囲

2. **因果連鎖エントリの `layer` フィールドが文字列（EnumではなくStringカラム）**
   - 無効な値を防ぐバリデーションはない
   - 現在 `"worker"` のみ記録しており、将来拡張時に不整合リスクがある
   - MVP段階では許容範囲

---

## 判定

**LGTM** — 全テストパス、仕様通りの実装、コードの可読性良好。
軽微な指摘は将来のリファクタリング対象として TODO に残すことを推奨。
